# BoostTestCmakeExample
I've tried to make an example where the BoostTest result shows up in the test explorer in both windows build and ubuntu build. using cmake to make a bit more generic build process
